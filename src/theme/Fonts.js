import { Platform } from 'react-native';
import Colors from './Colors';
import Metrics from './Metrics';

const type = {
  regular: 'Cochin',
  light: 'Cochin',
  bold: 'Cochin',
  semibold: 'Cochin',
};

const size = {
  h1: 30,
  h2: 22,
  h3: 20,
  h4: 18,
  h5: 16,
  h6: 14,
  default: 12,
  small: 10,
  mini: 8,
};

const style = {
  
};

export default {
  type,
  size,
  style,
};

